---
phase: 01-shared-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/admin/page.tsx
autonomous: true
requirements:
  - SHELL-02
  - SHELL-03

must_haves:
  truths:
    - "page.tsx imports TabKey, Player, PlayersResponse, RunHistoryEntry, RunHistoryStatusFilter, RunHistorySourceFilter, EmailPreviewMessage, EmailRerunOutcomeStatus, EmailRerunOutcome, EmailRerunChip, EmailRerunLog from admin/types"
    - "page.tsx imports formatDuration, formatIngestionHistorySummary, formatSplitwiseHistorySummary from admin/formatters"
    - "page.tsx no longer has inline definitions of the extracted types or formatter functions"
    - "Admin page renders and behaves identically (no regressions)"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Admin page using shared types and utilities"
      contains: "import.*from.*components/admin/types"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/components/admin/types.ts"
      via: "import statement"
      pattern: "import.*TabKey.*Player.*from"
    - from: "src/app/admin/page.tsx"
      to: "src/components/admin/formatters.ts"
      via: "import statement"
      pattern: "import.*formatDuration.*from"
---

<objective>
Wire page.tsx to use the shared types and utility modules created in Plan 01.

Purpose: Replace inline type definitions and formatter functions in page.tsx with imports from
the shared files, proving the extraction works and maintaining behavior parity.

Output: Modified page.tsx that imports from admin/types.ts and admin/formatters.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-foundation/01-CONTEXT.md
@.planning/phases/01-shared-foundation/01-01-SUMMARY.md

@src/app/admin/page.tsx (the file being modified)
@src/components/admin/types.ts (created in Plan 01 -- import source for types)
@src/components/admin/formatters.ts (created in Plan 01 -- import source for formatters)

<interfaces>
<!-- Types and exports from Plan 01 that this plan imports -->

From src/components/admin/types.ts:
```typescript
export type TabKey = 'accounts' | 'players' | 'club' | 'automation' | 'emails' | 'splitwise';
export type Player = { id: string; name: string; active: boolean; splitwise_user_id?: number | null; is_default_payer?: boolean; shuttlecock_paid?: boolean; avatar_path?: string | null; avatar_url?: string | null; };
export type PlayersResponse = { ok: boolean; players?: Player[]; error?: string; };
export type RunHistoryStatusFilter = 'ALL' | 'RUNNING' | 'SUCCESS' | 'FAILED' | 'SKIPPED';
export type RunHistorySourceFilter = 'ALL' | 'GITHUB_CRON' | 'ADMIN_MANUAL' | 'API' | 'UNKNOWN';
export type RunHistoryEntry = { id: string; job_type: 'INGESTION' | 'SPLITWISE'; run_source: RunHistorySourceFilter; status: Exclude<RunHistoryStatusFilter, 'ALL'>; started_at: string | null; finished_at: string | null; duration_ms: number | null; summary: Record<string, unknown> | null; error_message: string | null; created_at: string | null; };
export type EmailPreviewMessage = { id: string; rawHtml: string | null; rawText: string | null; htmlLength: number; textLength: number; htmlTruncated: boolean; textTruncated: boolean; status: 'NOT_INGESTED' | 'PARSE_FAILED' | 'SESSION_CREATED' | 'INGESTED_NO_SESSION'; parseError: string | null; parsedSessionDate: string | null; sessionId: string | null; sessionStatus: string | null; };
export type EmailRerunOutcomeStatus = 'INGESTED' | 'DEDUPED' | 'PARSE_FAILED' | 'FETCH_FAILED';
export type EmailRerunOutcome = { messageId: string; status: EmailRerunOutcomeStatus; reason: string | null; };
export type EmailRerunChip = { status: EmailRerunOutcomeStatus; text: string; tone: 'emerald' | 'amber' | 'rose'; };
export type EmailRerunLog = { summary: string; raw: unknown; };
```

From src/components/admin/formatters.ts:
```typescript
export function formatDuration(durationMs: number | null): string;
export function formatIngestionHistorySummary(summary: Record<string, unknown> | null): string;
export function formatSplitwiseHistorySummary(summary: Record<string, unknown> | null): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline types with imports from shared types file</name>
  <files>src/app/admin/page.tsx</files>
  <read_first>
    - src/app/admin/page.tsx (full file -- especially lines 1-15 for existing imports, lines 16-123 for type definitions to remove)
    - src/components/admin/types.ts (the shared types file to import from)
    - .planning/phases/01-shared-foundation/01-CONTEXT.md (D-01 lists which types move, D-02 lists which stay)
  </read_first>
  <action>
Modify `src/app/admin/page.tsx` to replace inline type definitions with imports from the shared types file. This involves two changes:

**Change 1: Add import statement**

Add this import after the existing component imports (after line 14, before the type definitions):

```typescript
import type {
  TabKey,
  Player,
  PlayersResponse,
  RunHistoryEntry,
  RunHistoryStatusFilter,
  RunHistorySourceFilter,
  EmailPreviewMessage,
  EmailRerunOutcomeStatus,
  EmailRerunOutcome,
  EmailRerunChip,
  EmailRerunLog,
} from '../../components/admin/types';
```

Use `import type` (not `import`) since these are type-only imports. Use relative path `../../components/admin/types` (project has no path aliases per CONVENTIONS.md).

**Change 2: Delete inline type definitions that were extracted**

Remove these type blocks from page.tsx (originally lines 16-123):
- `type TabKey = ...` (line 16)
- `type Player = { ... }` (lines 18-27)
- `type PlayersResponse = { ... }` (lines 29-33)
- `type RunHistoryStatusFilter = ...` (line 89)
- `type RunHistorySourceFilter = ...` (line 90)
- `type RunHistoryEntry = { ... }` (lines 92-103)
- `type EmailRerunOutcomeStatus = ...` (line 105)
- `type EmailRerunOutcome = { ... }` (lines 107-111)
- `type EmailRerunChip = { ... }` (lines 113-117)
- `type EmailRerunLog = { ... }` (lines 119-122)
- `type EmailPreviewMessage = { ... }` (lines 74-87)

**KEEP these tab-specific types inline** (per D-02):
- `type SplitwiseSettings = { ... }` (lines 35-45)
- `type AutomationSettings = { ... }` (lines 47-53)
- `type ClubTokenWarningCode = ...` (line 55)
- `type ClubTokenCurrentResponse = { ... }` (lines 57-65)
- `type ReceiptError = { ... }` (lines 67-72)

After this change, page.tsx should have about 11 fewer type definition blocks but all the same type names available via imports.
  </action>
  <verify>
    <automated>cd /Users/rouxsparrow/Code/club-genie && npx tsc --noEmit src/app/admin/page.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `import type {` followed by `TabKey` on same or next line
    - page.tsx contains `from '../../components/admin/types'`
    - page.tsx does NOT contain `type TabKey =` as a standalone definition (only in the import)
    - page.tsx does NOT contain `type Player = {` as a standalone definition
    - page.tsx does NOT contain `type PlayersResponse = {` as a standalone definition
    - page.tsx does NOT contain `type RunHistoryEntry = {` as a standalone definition
    - page.tsx does NOT contain `type EmailPreviewMessage = {` as a standalone definition
    - page.tsx does NOT contain `type EmailRerunOutcome = {` as a standalone definition
    - page.tsx STILL contains `type SplitwiseSettings =` (kept inline per D-02)
    - page.tsx STILL contains `type AutomationSettings =` (kept inline per D-02)
    - page.tsx STILL contains `type ClubTokenCurrentResponse =` (kept inline per D-02)
    - page.tsx STILL contains `type ReceiptError =` (kept inline per D-02)
    - `npx tsc --noEmit src/app/admin/page.tsx` exits 0
  </acceptance_criteria>
  <done>page.tsx imports all 11 cross-tab types from admin/types.ts; inline definitions removed; tab-specific types still inline; TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline formatters with imports and verify no regression</name>
  <files>src/app/admin/page.tsx</files>
  <read_first>
    - src/app/admin/page.tsx (read the current state after Task 1 -- look for the three const formatter functions inside AdminPage component body)
    - src/components/admin/formatters.ts (the shared formatters to import from)
  </read_first>
  <action>
Modify `src/app/admin/page.tsx` to replace inline formatter functions with imports.

**Change 1: Add formatter import**

Add this import alongside the types import (near top of file, in the imports section):

```typescript
import { formatDuration, formatIngestionHistorySummary, formatSplitwiseHistorySummary } from '../../components/admin/formatters';
```

This is a value import (not `import type`) since these are runtime functions.

**Change 2: Delete inline formatter definitions**

Remove these three `const` definitions from inside the `AdminPage` component body (originally lines 231-256, but line numbers will have shifted after Task 1):

```
const formatDuration = (durationMs: number | null) => { ... };
const formatIngestionHistorySummary = (summary: Record<string, unknown> | null) => { ... };
const formatSplitwiseHistorySummary = (summary: Record<string, unknown> | null) => { ... };
```

Search for these exact patterns to find them:
- `const formatDuration =`
- `const formatIngestionHistorySummary =`
- `const formatSplitwiseHistorySummary =`

The function bodies (the JSX that calls `formatDuration(entry.duration_ms)`, etc.) remain UNCHANGED. They will now call the imported module-level functions instead of the removed component-scoped constants.

**Final verification: Run typecheck and build to confirm no regressions.**
  </action>
  <verify>
    <automated>cd /Users/rouxsparrow/Code/club-genie && npx tsc --noEmit src/app/admin/page.tsx && npm test -- --run tests/admin-formatters.test.ts && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `import { formatDuration, formatIngestionHistorySummary, formatSplitwiseHistorySummary } from '../../components/admin/formatters'`
    - page.tsx does NOT contain `const formatDuration =`
    - page.tsx does NOT contain `const formatIngestionHistorySummary =`
    - page.tsx does NOT contain `const formatSplitwiseHistorySummary =`
    - page.tsx STILL contains calls like `formatDuration(entry.duration_ms)` (usage preserved)
    - page.tsx STILL contains calls like `formatIngestionHistorySummary(entry.summary)` (usage preserved)
    - page.tsx STILL contains calls like `formatSplitwiseHistorySummary(entry.summary)` (usage preserved)
    - `npx tsc --noEmit src/app/admin/page.tsx` exits 0
    - `npm test -- --run tests/admin-formatters.test.ts` exits 0
    - `npm run build` exits 0 (full Next.js build passes)
  </acceptance_criteria>
  <done>page.tsx imports all 3 formatters from admin/formatters.ts; inline definitions removed; all usages unchanged; TypeScript compiles; Next.js build passes; formatter tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only moves existing code from inline to imports -- no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | import paths | accept | Relative imports within same project; no external dependency introduced. Build verification confirms correct resolution. |
</threat_model>

<verification>
1. `npx tsc --noEmit src/app/admin/page.tsx` exits 0
2. `npm test -- --run tests/admin-formatters.test.ts` exits 0
3. `npm run build` exits 0 (full Next.js build confirms no broken imports)
4. page.tsx has import from `../../components/admin/types` with all 11 type names
5. page.tsx has import from `../../components/admin/formatters` with all 3 function names
6. No inline `type TabKey =`, `type Player =`, `const formatDuration =` etc. remain in page.tsx
7. Tab-specific types (SplitwiseSettings, AutomationSettings, etc.) still inline in page.tsx
</verification>

<success_criteria>
- page.tsx uses shared types and formatters via imports (no inline duplicates)
- All existing usages of types and formatters in JSX/logic are unchanged
- TypeScript compilation passes
- Next.js build passes (full regression check)
- Formatter unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-foundation/01-02-SUMMARY.md`
</output>
