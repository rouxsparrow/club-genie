---
phase: 03-shell-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/page.tsx
  - src/components/admin/admin-tab-shell.tsx
  - tests/admin-tab-extraction.test.ts
  - TASKS.md
autonomous: false
requirements:
  - SHELL-01
  - PARITY-07
must_haves:
  truths:
    - "The /admin route still switches tabs using local activeTab state with no query-param or router tab sync."
    - "Tabs that are currently eager-mounted (players, club, automation, splitwise) still mount on first load, while accounts and emails mount on first visit and stay mounted after that."
    - "src/app/admin/page.tsx stays a shell-only route entry under 150 lines with no inline tab button duplication or per-tab panel duplication."
  artifacts:
    - path: "src/components/admin/admin-tab-shell.tsx"
      provides: "Shared admin tab metadata plus mapped navigation/panel rendering helpers"
    - path: "src/app/admin/page.tsx"
      provides: "Thin admin route shell that owns mounted/activeTab/visitedTabs only"
    - path: "tests/admin-tab-extraction.test.ts"
      provides: "Source-level assertions that lock the Phase 3 shell contract"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/components/admin/admin-tab-shell.tsx"
      via: "imported tab registry and shared render helpers"
      pattern: "from '../../components/admin/admin-tab-shell'"
    - from: "src/app/admin/page.tsx"
      to: "src/components/admin/types.ts"
      via: "TabKey-typed activeTab and visitedTabs state"
      pattern: "useState<TabKey>|Record<TabKey, boolean>"
    - from: "tests/admin-tab-extraction.test.ts"
      to: "src/app/admin/page.tsx"
      via: "source assertions for eager tabs, shell-only imports, and no query-param routing"
      pattern: "readFileSync\\(pagePath"
---

<objective>
Reduce `src/app/admin/page.tsx` to a thin composition shell without changing current admin tab behavior.

Purpose: Close `SHELL-01` and `PARITY-07` with the smallest possible diff by extracting repeated shell markup, not by redesigning navigation or state.
Output: A shared admin tab shell helper, a sub-150-line `/admin` page route, and source-level proof that local-state tab semantics stay intact.
</objective>

<execution_context>
@/Users/rouxsparrow/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rouxsparrow/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shell-reduction/03-RESEARCH.md
@.planning/phases/02-tab-extraction/02-VERIFICATION.md
@src/app/admin/page.tsx
@src/components/admin/types.ts
@tests/admin-tab-extraction.test.ts
@TASKS.md

<interfaces>
From `src/components/admin/types.ts`:

```ts
export type TabKey = 'accounts' | 'players' | 'club' | 'automation' | 'emails' | 'splitwise';
```

From the current `src/app/admin/page.tsx` shell contract:

```tsx
const [activeTab, setActiveTab] = useState<TabKey>('players');
const eagerMountedTabs: TabKey[] = ['players', 'club', 'automation', 'splitwise'];
const [visitedTabs, setVisitedTabs] = useState<Record<TabKey, boolean>>({
  accounts: false,
  players: true,
  club: false,
  automation: false,
  emails: false,
  splitwise: true,
});
```

Phase 3 must preserve that contract exactly: local state only, keep-mounted visited tabs, eager-mounted `players`/`club`/`automation`/`splitwise`, and no query-param tab routing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the shell-parity contract in source-level tests</name>
  <read_first>
    - tests/admin-tab-extraction.test.ts
    - src/app/admin/page.tsx
    - TASKS.md
    - .planning/phases/03-shell-reduction/03-RESEARCH.md
  </read_first>
  <files>tests/admin-tab-extraction.test.ts, TASKS.md</files>
  <behavior>
    - Test 1: `src/app/admin/page.tsx` keeps `useState<TabKey>('players')`, the current eager-mounted tab set, and `Record<TabKey, boolean>` visited-tab state.
    - Test 2: `src/app/admin/page.tsx` does not introduce `useSearchParams`, `router`, or search-param parsing for tab state.
    - Test 3: Phase 3 test rationale is recorded in `TASKS.md`, noting source-level automation plus manual parity verification because the repo has no DOM harness.
  </behavior>
  <action>Extend `tests/admin-tab-extraction.test.ts` with a new Phase 3 shell-reduction suite that reads `src/app/admin/page.tsx` and grep-locks the preserved shell semantics per `PARITY-07`: local `activeTab`, current eager-mounted set, keep-mounted visited-tab record, no router/search-param tab sync, and an import of the extracted shell helper rather than duplicated per-tab button/panel blocks. Update `TASKS.md` with a single Phase 3 entry that explains the validation mix for this parity-preserving shell cleanup.</action>
  <acceptance_criteria>
    - `rg -n "phase 03 shell reduction" tests/admin-tab-extraction.test.ts` returns the new suite heading.
    - `rg -n "useState<TabKey>\\('players'\\)|\\['players', 'club', 'automation', 'splitwise'\\]|Record<TabKey, boolean>" tests/admin-tab-extraction.test.ts` finds the locked shell-contract assertions.
    - `rg -n "useSearchParams|router\\\\.|searchParams" tests/admin-tab-extraction.test.ts` shows explicit negative assertions or expectations for the absence of URL-based tab state.
    - `rg -n "Phase 03 shell reduction: page.tsx becomes a thin mapped tab shell" TASKS.md` finds the new test rationale entry.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/admin-tab-extraction.test.ts</automated>
  </verify>
  <done>`tests/admin-tab-extraction.test.ts` fails before the shell refactor and precisely describes the Phase 3 parity contract, while `TASKS.md` documents the required validation rationale for the functional refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared tab-shell rendering and slim page.tsx below 150 lines</name>
  <read_first>
    - src/app/admin/page.tsx
    - src/components/admin/types.ts
    - tests/admin-tab-extraction.test.ts
    - .planning/phases/03-shell-reduction/03-RESEARCH.md
  </read_first>
  <files>src/components/admin/admin-tab-shell.tsx, src/app/admin/page.tsx</files>
  <behavior>
    - Test 1: `src/app/admin/page.tsx` remains shell-only, importing shared tab metadata/render helpers instead of inlining six buttons and six panel wrappers.
    - Test 2: `activeTab`, `mounted`, `visitedTabs`, and the eager-mounted set behave exactly as before, including `emails` lazy-first-visit and `splitwise` eager-first-load semantics.
    - Test 3: `src/app/admin/page.tsx` ends under 150 lines without adding query-param routing or moving business logic back into the route.
  </behavior>
  <action>Create `src/components/admin/admin-tab-shell.tsx` as the shared shell-only helper module for Phase 3. Put the tab registry, display labels, eager-mounted metadata, and mapped navigation/panel rendering helpers there, using the existing tab component imports and current visual classes unchanged. Then rewrite `src/app/admin/page.tsx` to keep only route concerns: `mounted`, `activeTab`, `visitedTabs`, the visited-tab update effect, the loading shell, and composition of the extracted helpers. Preserve current tab order, labels, eager-mounted tabs (`players`, `club`, `automation`, `splitwise`), keep-mounted visited-tab behavior, and the current no-query-param tab model exactly per `SHELL-01` and `PARITY-07`.</action>
  <acceptance_criteria>
    - `test $(wc -l < src/app/admin/page.tsx) -lt 150` succeeds.
    - `rg -n "from '../../components/admin/admin-tab-shell'" src/app/admin/page.tsx` finds the shared shell import.
    - `rg -n "useSearchParams|router\\\\.|searchParams" src/app/admin/page.tsx src/components/admin/admin-tab-shell.tsx` returns no matches.
    - `rg -n "players|club|automation|splitwise" src/components/admin/admin-tab-shell.tsx` confirms the eager-mounted metadata lives in the helper module.
    - `rg -n "AccountsTab|PlayersTab|ClubAccessTab|AutomationTab|EmailsTab|SplitwiseTab" src/components/admin/admin-tab-shell.tsx` confirms the helper owns tab component composition.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/admin-tab-extraction.test.ts && npm run lint && npm run typecheck</automated>
  </verify>
  <done>`src/app/admin/page.tsx` is a sub-150-line shell-only route, shared tab rendering lives in `src/components/admin/admin-tab-shell.tsx`, and automated checks prove the admin shell still follows the pre-Phase-3 navigation semantics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify admin tab navigation parity in the browser</name>
  <read_first>
    - src/app/admin/page.tsx
    - src/components/admin/admin-tab-shell.tsx
    - TASKS.md
  </read_first>
  <files>src/app/admin/page.tsx, src/components/admin/admin-tab-shell.tsx, TASKS.md</files>
  <acceptance_criteria>
    - Visiting `/admin` lands on Players first without a query param.
    - Players, Club Access, Automation, and Splitwise still feel preloaded on first page load; Accounts and Email Preview still mount when first opened and retain state after switching away and back.
    - Tab highlight, labels, and content switching match the current admin UI with no redesign drift.
  </acceptance_criteria>
  <action>After the automated refactor passes, open `/admin` and verify the shell behavior end-to-end: initial Players tab selection, current tab labels/order, eager preload feel for `players`/`club`/`automation`/`splitwise`, lazy-first-visit behavior for `accounts` and `emails`, and hidden-tab persistence after switching back and forth. If any tab remounts unexpectedly or URL/query state appears, stop and fix before marking the plan complete.</action>
  <verify>
    <automated>MISSING — use the automated commands from Task 2 before running this browser parity checkpoint</automated>
  </verify>
  <done>Human verification confirms the shell cleanup did not change tab routing semantics or keep-mounted persistence, so `PARITY-07` is satisfied in the live UI as well as in source-level checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin route shell -> tab helper module | Untrusted regressions could change which tab renders or mounts without touching tab business logic |
| admin click event -> local shell state | User input drives `activeTab` and visited-tab transitions, so parity-breaking routing logic must not be introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `src/components/admin/admin-tab-shell.tsx` | mitigate | Keep tab keys typed with `TabKey`, encode the eager-mounted set in one helper module, and lock those strings in `tests/admin-tab-extraction.test.ts` so shell metadata cannot drift silently |
| T-03-02 | D | `src/app/admin/page.tsx` | mitigate | Preserve keep-mounted visited-tab behavior and test the exact eager/lazy semantics so the shell refactor does not accidentally remount heavy tabs or lose hidden-tab state |
| T-03-03 | S | `/admin` tab routing model | accept | This phase preserves the existing local-state-only routing model and does not introduce URL-addressable tabs, so there is no new auth or navigation spoofing surface beyond the current UI behavior |
</threat_model>

<verification>
Run `npm test -- --run tests/admin-tab-extraction.test.ts`, `npm run lint`, and `npm run typecheck`, then complete the `/admin` browser parity walkthrough described in Task 3.
</verification>

<success_criteria>
- `src/app/admin/page.tsx` is under 150 lines and contains only shell state plus composition wiring.
- Shared tab metadata/rendering lives in `src/components/admin/admin-tab-shell.tsx`.
- The current local `activeTab` model, eager-mounted tab set, keep-mounted visited-tab behavior, and no-query-param navigation semantics are preserved.
- `SHELL-01` and `PARITY-07` are both fully covered by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-reduction/03-shell-reduction-01-SUMMARY.md`
</output>
