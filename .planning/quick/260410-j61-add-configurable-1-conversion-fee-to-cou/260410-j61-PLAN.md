# Quick Task 260410-j61: Add configurable COURT Splitwise conversion fee

## Goal
Add configurable conversion fee percent for COURT Splitwise expense only, default 1%, configurable in Admin Splitwise tab.

## Plan
1. Add DB migration column `splitwise_settings.court_conversion_fee_percent numeric(6,2) not null default 1.00`.
2. Extend admin settings API and Splitwise tab UI to read/write the value.
3. Extend run-splitwise-sync settings loader and COURT amount calculation so only COURT uses cost plus conversion fee; SHUTTLECOCK remains unchanged.
4. Add focused tests for fee math and UI/source contract.
5. Run targeted tests, lint, typecheck, then commit atomically.
