import type { ReactNode } from "react";

export const metadata = {
  title: "Club Genie - Sessions V2",
  description: "Find and join badminton sessions",
};

export default function SessionsV2Layout({ children }: { children: ReactNode }) {
  return children;
}
